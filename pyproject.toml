[tool.poetry]
name = "dog_school"
version = "0.1.0"
description = ""
authors = ["Romulo Collopy <romulo.collopy@freshbooks.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
